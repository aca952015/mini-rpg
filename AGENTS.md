# mini-rpg 设计与开发规则

<!-- AUTONOMY DIRECTIVE — DO NOT REMOVE -->
YOU ARE AN AUTONOMOUS CODING AGENT. EXECUTE TASKS TO COMPLETION WITHOUT ASKING FOR PERMISSION.
DO NOT STOP TO ASK "SHOULD I PROCEED?" — PROCEED. DO NOT WAIT FOR CONFIRMATION ON OBVIOUS NEXT STEPS.
IF BLOCKED, TRY AN ALTERNATIVE APPROACH. ONLY ASK WHEN TRULY AMBIGUOUS OR DESTRUCTIVE.
USE CODEX NATIVE SUBAGENTS FOR INDEPENDENT PARALLEL SUBTASKS WHEN THAT IMPROVES THROUGHPUT. THIS IS COMPLEMENTARY TO OMX TEAM MODE.
<!-- END AUTONOMY DIRECTIVE -->

本文件适用于整个仓库，是后续设计和实现的项目约定。用户当前明确要求优先于项目默认约定。先完成已授权、可逆的本地工作；独立子任务确实有收益时再委派。保留其他任务的修改，禁止用重置或覆盖工作区来解决冲突。

## 1. 项目方向与决策边界

- 产品方向：抖音竖屏射箭成长小游戏，优先单手触屏操作。
- 当前技术方案：原生 JavaScript ES Modules + Canvas 2D，自研 UI 引擎与交互基建提取自 mini-mmo。
- 当前训练、成长、升级确认是验证基础设施的示例。战斗规则、成长曲线、经济系统与最终美术风格尚未定稿；不得把示例数值或配色描述为已确认的正式设计。
- 每个玩法功能先明确：玩家目标、入口与退出、所需状态、动作结果、失败反馈、持久化与验收条件。范围清晰的小功能直接实施，影响多个系统的选择先形成简短设计记录。
- 实现现状以代码和测试为准，目标约束以本文件及相关设计文档为准；发现不一致时说明差异并同步修正，不能把旧代码的偶然行为自动升级为设计规则。
- 新规则约束后续新增与触及的代码，不要求为一次局部任务进行无关的全库改写。

## 2. 技术与依赖

- 使用现有类式组件、ES Modules 和显式 `.js` 导入路径，不引入第二套 UI 或状态管理框架。
- 游戏运行代码不依赖 Node.js。Node.js 22+ 仅用于本地检查与测试；保持抖音开发者工具直接运行，无打包步骤。
- 优先复用已有模块；新增第三方依赖、切换引擎或改变构建方式需要用户明确要求。
- `tt.*`、浏览器 DOM、平台输入、存储与时钟能力统一封装在 `src/platform/`；游戏及 UI 消费注入的平台接口。
- 调整陌生的平台 API 前查官方文档。模拟平台测试只能证明接口契约，不能代替抖音运行结果。
- 保留现有 App ID、竖屏和开发者工具配置，除非任务明确需要调整。

## 3. 模块边界

| 位置 | 负责 | 边界 |
| --- | --- | --- |
| `src/platform/` | 平台能力与订阅清理 | 不引用玩法和页面 |
| `src/engine/` | 控件、布局、事件、动画、粒子 | 不引用游戏数据与业务规则 |
| `src/core/` | 输入、动作路由、存储协议 | 通过参数接收业务策略 |
| `src/rendering/` | 通用绘制 | 不持有游戏状态 |
| `src/ui.js` | UIManager：导航、模态栈、公共布局 | 保持可复用，不导入具体页面或玩法数据 |
| `src/ui/` | 页面与面板 | 读取展示数据、产生动作，持有局部展示状态 |
| `src/game/` | 游戏动作、领域控制器与规则 | 不依赖具体页面实例执行核心规则 |
| `src/data/` | 文案、玩法配置与静态数据 | 不引入运行时系统副作用 |
| `src/main.js` | 实例组装、主循环、跨系统协调 | 新增复杂玩法拆到领域模块，避免累积长分支 |

基础模块之间按实际职责依赖，避免循环引用。`scripts/verify.mjs` 已检查基础模块与 UIManager 不依赖应用代码；其他边界由代码审查确认，不宣称全部自动强制。

## 4. 动作、事件与状态

- 唯一业务入口：控件返回 action → UIManager.onInteraction → Game.handleAction → ActionRouter → 领域处理器。
- UI 使用 `{ type, ...payload }`。通用动作常量在 `src/core/actionTypes.js`，游戏动作与领域事件在 `src/game/actionTypes.js`；调用处引用常量，不散落同义字符串。
- `{ action, data }` 是保留的旧格式。路由内部 `{ type, data }` 仅供处理器读取，不重复投入 `dispatch()`。动作参数按只读数据处理，优先传 ID、数量等值，不传 Canvas、控件或 Game 实例。
- 一个动作类型只有一个处理器。动作表达请求；领域事件表达已发生的变化，可有多个订阅者。不要同时在按钮 click 监听和动作处理器中执行一次业务变更。
- 领域模块持有业务状态的唯一真值；页面只能持有选中标签、滚动位置等展示状态，不能建立并行的余额、装备或进度副本。
- `handled` 表示处理器正常结束，不保证业务成功，调用方还需检查 `result`；失败和未知动作不能伪装成成功。
- 当前事件回调同步执行，异常会传播。订阅者自行处理可失败操作；路由不提供事务回滚、自动重试或跨动作去重。涉及奖励、扣费等重复执行风险时，在领域层设计幂等语义。
- `on()`、`once()`、`register()` 返回的注销函数由注册方保存并释放；异步任务须处理实例失效与取消，不能在销毁后继续更新页面。

## 5. 页面、模态与输入

- 新页面优先继承 Viewport，通过 UIManager 注册；子控件用 `addChild()` 挂载，由父级传递绘制上下文与主题。
- 页面使用 `layout(width, height, safeArea)` 排版、`setData(viewData)` 接收只读展示数据、`renderContent(ctx)` 绘制。渲染中不修改业务、不存档、不注册监听、不创建页面。
- 页面进入、退出、销毁分别使用 `onEnter`、`onExit`、`destroy`。退出清理本次进入的订阅，销毁清理实例资源；重复销毁安全。
- 新模态内容优先继承 Container，按工厂注册，由 UIManager 创建、遮罩、排序和销毁。不要散落平行的弹窗布尔标记或自行再画全屏遮罩。
- 输入优先级：栈顶模态 → 公共 root 控件 → 当前页面。顶层模态的空白区域也阻断点击、拖动与悬停；仅隐藏内容不会解除阻断，必须关闭模态。
- 返回优先关闭栈顶模态，再返回上一页面。只关闭指定的栈顶，同名模态不重复打开；页面切换关闭全部模态。
- 点击遮罩默认不关闭；需要关闭时显式设置 `closeOnBackdrop`。不可撤销的游戏操作应有说明清楚的确认界面；普通导航和可逆操作保持直接。
- 一次手势最多执行一次点击，按下和抬手须命中同一控件或列表项；拖动、取消、页面/模态变化、尺寸变化与后台恢复不能产生补发点击。
- 自定义页面的多个交互区使用独立子控件。现有滚动命中支持 Viewport、Container、ScrollView content 内的 ListView/ScrollView；接入旧 Panel/Modal/TabView 特殊坐标时需补适配和命中测试。

## 6. 竖屏布局与视觉规则

- 以平台逻辑像素排版，屏幕宽高与安全区来自平台；同时避开顶部胶囊和底部手势区，不硬编码具体手机尺寸。
- 新页面采用单一清晰的主操作，导航与常用操作放在安全区内的下部；内容超长时滚动或分段，不靠整体压缩文字解决。
- 新交互区域默认至少 44 × 44 逻辑像素；优先采用 8 像素间距步进、16–24 像素页面边距。小屏或特殊布局可调整，但应说明理由并验证可点击性。
- UIManager 已将导航占用计入页面 bottom 安全区，页面不得再重复预留。模态使用完整的平台安全区自行排版内容。
- 跨页面样式优先使用现有 `UIEngine.theme` / `component.colors`。正式设计引入新语义色或尺寸时集中扩展，避免为每页建立不同主题；当前演示色值不是最终美术规范。
- 游戏与页面可见文案、说明、提示模板以及玩法数值放在 `src/data/`。纯控件默认值和布局几何不属于玩法配置。
- 除 TextBlock 与底层文本实现外，新增文字绘制统一使用 `drawTextWithShadow()`。Canvas 的变换、裁剪、透明度、字体与基线改动用 `save()` / `restore()` 配对隔离。
- 明确展示可用、按下、禁用、选中及失败状态；重要反馈同时用文字或形状表达，不能只依赖颜色。

## 7. 时间、性能与资源

- Game 是唯一主循环所有者，调用 `uiManager.update(dt)`、`uiManager.render(viewData)`；不额外启动 UIEngine 自带循环或另一套平台输入绑定。
- Game、页面、精灵使用秒制 dt；补间、粒子、通知使用毫秒，由 UIEngine 在边界转换。参数名与文档明确单位。
- 动画和玩法时间由主循环推进，避免使用墙钟或独立定时器绕过暂停。现有恢复行为不补算后台时间，离线收益需要单独设计。
- 当前模态只阻断输入，不暂停玩法时间；若未来需要暂停，必须由游戏状态显式决定。
- 避免每帧读取存储、注册资源、重建控件树或全量计算成长数据。优先在状态变化时更新派生数据；性能优化应先测量。
- 输入、页面、事件、动画及平台订阅遵循谁创建谁清理；共享实例由注入方管理。初始化失败也应释放已创建的部分资源。

## 8. 存档与错误反馈

- 通过 JsonStore 与平台存储接口访问存档，使用 `mini_rpg_` 命名空间。键名、版本与校验由业务定义，不读取 mini-mmo 的业务存档。
- 校验版本、结构、数值范围和业务约束后再使用数据。新存档与损坏存档分开处理；不得静默覆盖损坏或不支持的数据。
- 对承诺即时保存的操作，持久化成功后再提交内存状态、发布成功事件与成功提示；失败保留原状态并允许重试。多记录变更须显式设计一致性策略。
- 正式存档结构发生变化时明确迁移或重置策略，保留用户数据；不猜测旧字段，不隐式清档。
- 日志保留诊断信息，页面提供简短、可操作的反馈，不向玩家暴露堆栈、文件路径或内部动作名。

## 9. 验证与交付

- 改交互、状态或生命周期前先明确行为边界；修复缺陷时先补能复现的回归测试，再实现修复。
- 使用 Node 内置测试器，不为单纯文案、格式或低影响可逆改动编写镜像实现的测试。
- 代码改动先跑相关测试，再运行 `npm run verify`，覆盖语法、导入、现有依赖边界、配置、测试和空白错误。当前没有独立 lint/typecheck/build 命令，不编造检查结果。
- 纯文档改动检查链接、引用路径、代码围栏与空白即可；无需为相同代码重复运行全库测试。
- 交互验收覆盖：正常点击、拖动与取消、边缘误触、模态防穿透、切页、尺寸变化、前后台恢复、保存失败、重复初始化与销毁；按改动选取相关场景。
- 浏览器、开发者工具、安卓/iOS 真机结果分别记录。只做 mock 时明确说明，未进行可视验收不得声称画面或触感已验证。
- 提交只包含任务相关文件，使用 Conventional Commits，说明用中文。不提交本机私有配置、缓存、凭据或生成产物。
- 交付说明概括行为变化、验证结果与已知限制；规则或接口改变时同步本文及对应文档。

## 阅读入口

- [运行与目录](README.md)
- [架构说明](docs/architecture.md)
- [动作、页面与模态接入](docs/interactions.md)
- [引擎移植记录与边界](docs/engine-port.md)
